<div class="worklist-list__work-name-header">{{ @intl.patients.worklist.tableHeaderTemplate.patientHeader }}</div>
<div class="worklist-list__header-span-2">
  {{#if isFlowList}}
    {{ @intl.patients.worklist.tableHeaderTemplate.flowHeader }}
  {{ else }}
    {{ @intl.patients.worklist.tableHeaderTemplate.actionHeader }}
  {{/if}}
</div>
{{#if isFlowList}}
  <div>{{ @intl.patients.worklist.tableHeaderTemplate.flowAttrHeader }}</div>
{{else}}
  <div class="worklist-list__action-attr-header">{{ @intl.patients.worklist.tableHeaderTemplate.actionAttrHeader }}</div>
{{/if}}
<div>{{ @intl.patients.worklist.tableHeaderTemplate.datesHeader }}</div>
